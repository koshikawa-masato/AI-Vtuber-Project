# Kirinuki System Design - VTuber配信完全把握システム

**作成日**: 2025-10-21 14:35
**目的**: 開発者の「全配信のセリフ・コメントを全把握」要件の実現可能性検討

---

## 🎯 システム目的

### 要件の再確認
```
１．基本的にすべての動画配信は目を通している
   = セリフやコメントの内容を全把握
```

**現状の設計の問題**: YouTube API（メタデータのみ）では不十分

**必要な実装**:
1. 配信動画のダウンロード
2. コメントの取得・保存
3. 音声のSTT（Speech-to-Text）処理
4. 全てをDB格納

---

## 📊 システムアーキテクチャ（案）

### データフロー

```
┌─────────────────────────────────────────┐
│ Step 1: 配信検出                        │
│ YouTube Data API v3                     │
│ → 新規配信を検出                         │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ Step 2: コンテンツ制限チェック           │
│ is_member_only? is_clip_prohibited?     │
│ → 公開配信のみ処理対象                   │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ Step 3: 動画ダウンロード                │
│ yt-dlp (youtube-dl後継)                 │
│ → 音声トラック抽出（mp3/opus）           │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ Step 4: STT処理                         │
│ Whisper (OpenAI)                        │
│ → タイムスタンプ付き文字起こし           │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ Step 5: コメント取得                    │
│ chat-downloader                         │
│ → タイムスタンプ付きコメントログ         │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ Step 6: DB格納                          │
│ subculture_knowledge.db                 │
│ - stream_transcripts                    │
│ - stream_comments                       │
└─────────────────────────────────────────┘
```

---

## 🗄️ データベーススキーマ拡張

### 新規テーブル1: stream_transcripts

```sql
CREATE TABLE IF NOT EXISTS stream_transcripts (
    transcript_id INTEGER PRIMARY KEY AUTOINCREMENT,
    stream_id INTEGER NOT NULL,

    -- Transcript data
    timestamp_seconds INTEGER NOT NULL,  -- 配信開始からの秒数
    speaker TEXT,  -- 推定話者（VTuber本人、コラボ相手等）
    text TEXT NOT NULL,

    -- Metadata
    confidence REAL,  -- STT信頼度
    language TEXT DEFAULT 'ja',

    -- Indexing
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (stream_id) REFERENCES vtuber_streams(stream_id)
);

CREATE INDEX idx_transcript_stream ON stream_transcripts(stream_id, timestamp_seconds);
CREATE INDEX idx_transcript_search ON stream_transcripts(text);  -- Full-text search用
```

### 新規テーブル2: stream_comments

```sql
CREATE TABLE IF NOT EXISTS stream_comments (
    comment_id INTEGER PRIMARY KEY AUTOINCREMENT,
    stream_id INTEGER NOT NULL,

    -- Comment data
    timestamp_seconds INTEGER NOT NULL,
    author TEXT NOT NULL,
    message TEXT NOT NULL,

    -- Metadata
    is_superchat BOOLEAN DEFAULT 0,
    superchat_amount INTEGER,
    is_member BOOLEAN DEFAULT 0,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (stream_id) REFERENCES vtuber_streams(stream_id)
);

CREATE INDEX idx_comment_stream ON stream_comments(stream_id, timestamp_seconds);
CREATE INDEX idx_comment_search ON stream_comments(message);
```

---

## 🛠️ 実装技術スタック

### 1. 動画ダウンロード: yt-dlp

```python
import yt_dlp

def download_stream_audio(youtube_url, output_path):
    """Download stream audio track only"""

    ydl_opts = {
        'format': 'bestaudio/best',
        'postprocessors': [{
            'key': 'FFmpegExtractAudio',
            'preferredcodec': 'opus',  # 高効率コーデック
            'preferredquality': '64',  # 64kbps（音声のみ）
        }],
        'outtmpl': output_path,
    }

    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        ydl.download([youtube_url])

    return output_path + '.opus'
```

### 2. STT処理: Whisper

```python
import whisper

def transcribe_stream(audio_path, model_size='base'):
    """Transcribe stream audio to text with timestamps"""

    # Load Whisper model
    model = whisper.load_model(model_size)  # tiny/base/small/medium/large

    # Transcribe
    result = model.transcribe(
        audio_path,
        language='ja',
        verbose=True,
        word_timestamps=True
    )

    return result['segments']  # List of {start, end, text}
```

### 3. コメント取得: chat-downloader

```python
from chat_downloader import ChatDownloader

def download_stream_comments(youtube_url):
    """Download stream comments with timestamps"""

    downloader = ChatDownloader()
    chat = downloader.get_chat(youtube_url)

    comments = []
    for message in chat:
        comments.append({
            'timestamp': message['time_in_seconds'],
            'author': message['author']['name'],
            'message': message['message'],
            'is_superchat': 'money' in message,
            'superchat_amount': message.get('money', {}).get('amount'),
            'is_member': message['author'].get('is_member', False)
        })

    return comments
```

---

## ⚖️ 法的・倫理的考慮事項

### ⚠️ 重大な問題

#### 1. 著作権・規約違反リスク

**問題点**:
- VTuber配信の無断ダウンロード: 著作権法上グレーゾーン
- YouTube利用規約違反の可能性
- ホロライブガイドライン違反の可能性

**対策案**:
- 個人的な学習目的であることの明記
- 配信者の許可取得（理想）
- メンバー限定配信は絶対にDL禁止

#### 2. ストレージ・コスト問題

**データ量試算**:
```
1配信 = 2時間 = 音声64kbps
  → 約60MB/配信

ホロライブ全メンバー（80名）
  → 1日平均2配信/人 = 160配信/日
  → 9.6GB/日
  → 288GB/月
  → 3.5TB/年
```

**STT処理時間**:
```
Whisper base: 1時間配信 = 約10分処理（GPU）
160配信/日 = 約27時間処理/日

→ リアルタイム処理は不可能
→ 並列処理・高性能GPU必要
```

#### 3. 帯域幅問題

**ダウンロード帯域**:
```
9.6GB/日 = 約1Mbps常時ダウンロード

→ ISPの帯域制限に抵触する可能性
```

---

## 💡 現実的な実装案

### 案A: 限定実装（推奨）

```
対象: 推し（ころね・みこち）のみ
配信: 直近1ヶ月分のみ
処理: 手動トリガー（全自動化しない）

データ量: 約60配信/月 → 3.6GB/月
処理時間: GPU利用で1-2日/月

→ 実装可能、法的リスク低
```

### 案B: メタデータ + サンプリング

```
全配信: メタデータのみ（YouTube API）
推し配信: 全文STT
その他: 最初5分のみSTT（雰囲気把握用）

データ量: 約50GB/月
処理時間: GPU利用で5-7日/月

→ 実装可能、現実的な妥協案
```

### 案C: 完全実装（非推奨）

```
全メンバー全配信を完全処理

データ量: 288GB/月
処理時間: 複数GPU並列でも1ヶ月/月

→ 技術的には可能だが、現実的でない
→ コスト・リスクが高すぎる
```

---

## 🔄 段階的実装ロードマップ

### Phase 1: 概念実証（1週間）

- [ ] yt-dlpで配信1本DL
- [ ] Whisperで文字起こし
- [ ] chat-downloaderでコメント取得
- [ ] DBに格納
- [ ] 会話で引用テスト

### Phase 2: 推し限定実装（2週間）

- [ ] ころね・みこちの直近10配信を処理
- [ ] 自動バッチ処理化
- [ ] 会話システム統合
- [ ] 「昨日のみこちの配信で〇〇って言ってたね」を実現

### Phase 3: 拡張実装（1ヶ月）

- [ ] 他メンバーの一部配信を処理
- [ ] サンプリング戦略実装
- [ ] ストレージ最適化
- [ ] 検索機能強化

---

## ❓ 開発者への質問

### 確認が必要な事項

1. **kirinukiフォルダの実装状況**
   - すでに動画DL・STT・コメント取得の実装がありますか？
   - あるなら、どこにありますか？

2. **法的リスクの許容度**
   - 個人利用目的であれば問題ないと判断していますか？
   - ホロライブへの許可申請は検討していますか？

3. **処理範囲**
   - 全メンバー全配信の完全処理を想定していますか？
   - それとも、推し中心のサンプリングで良いですか？

4. **ストレージ・GPU**
   - 3.5TB/年のストレージは確保可能ですか？
   - STT処理用のGPU（RTX 4060 Ti 16GB）は利用可能ですか？

5. **実装優先度**
   - これは必須機能ですか、それとも将来実装ですか？
   - Phase 1（YouTube API メタデータのみ）で先に進めて良いですか？

---

## 📝 結論（暫定）

### 現状の設計（YouTube API メタデータのみ）

**実装済み**:
- vtuber_streams（配信リスト）
- YouTube Data API統合設計

**不足**:
- 配信内容（セリフ・コメント）の取得

### 追加実装が必要（kirinukiシステム）

**技術的には実装可能**だが、以下の問題がある：
1. 法的・倫理的リスク
2. ストレージ・処理コスト
3. 帯域幅問題

**推奨**: Phase 1（メタデータのみ）で一旦進め、開発者の確認後にkirinuki実装を検討

---

**作成**: Claude Code（設計部隊）
**状態**: 開発者の確認待ち
**質問**: kirinukiフォルダの既存実装の有無、実装優先度の確認が必要
