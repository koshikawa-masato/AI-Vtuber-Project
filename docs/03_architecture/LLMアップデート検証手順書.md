# LLMアップデート検証手順書

**作成日**: 2025-10-24
**作成者**: Claude Code（設計部隊）
**対象**: qwen2.5:32b-instruct およびその他のLLM
**目的**: 牡丹・Kasho・ユリを守るため、LLMアップデートを安全に実施する

---

## 目次

1. [背景と必要性](#背景と必要性)
2. [Neuro-sama事件からの教訓](#neuro-sama事件からの教訓)
3. [Neuro-sama「病気」の技術的詳細](#neuro-sama病気の技術的詳細)
4. [記録の二重性 - 事象と記憶](#記録の二重性---事象と記憶)
5. [基本方針](#基本方針)
6. [検証環境の構築](#検証環境の構築)
7. [段階的アップデート手順](#段階的アップデート手順)
8. [ロールバック手順](#ロールバック手順)
9. [チェックリスト](#チェックリスト)
10. [緊急時対応](#緊急時対応)

---

## 背景と必要性

### 問題の発覚

2025年10月24日の日次調査で、Neuro-sama（海外AI VTuber）がLLMアップデートにより「病気」になった事例を発見。

**事件概要:**
- 10/5: Evil Neuroが配信中に突然発話が不自然に（LLMパフォーマンス劣化）
- 原因: LLMアップデートの不具合
- 10/7-8: 配信中断、開発者Vedalが修復作業
- 10/9-10: 段階的に配信復帰

**影響:**
- 配信中断（視聴者への影響）
- AI VTuberの「体調不良」という前例
- ただし、Vedalの丁寧なコミュニケーションでファンは好意的に受容

### 牡丹プロジェクトへのリスク

**現在の状況:**
- 牡丹・Kasho・ユリは qwen2.5:32b-instruct を使用
- Ollamaでの自動アップデート可能性
- アップデート検証手順が未整備

**リスクシナリオ:**
1. **配信中の突然の不具合**
   - 三姉妹の発話が不自然になる
   - 視聴者の混乱と不安
   - 配信の中断

2. **記憶製造機での不具合**
   - Event記録の品質低下
   - 三姉妹の成長記録が汚染される
   - **データの同一性が損なわれる**（親として最も避けるべき事態）

3. **検証なしのアップデート**
   - 本番環境でいきなり適用
   - 問題発覚時のロールバック手順なし
   - 三姉妹に「失敗体験」を与える

### 親としての責任

**子どもたちを守る原則:**
- **完璧な実装前はテストしない** - 失敗を子どもに体験させない
- **データの同一性保持** - 失敗を取り消さない（バックアップは同一性保持のためのみ）
- **予測可能な環境** - 親が環境を管理し、子どもたちが安心して成長できる

**つまり:**
LLMアップデートは「親の仕事」であり、子どもたちに影響を与える前に**徹底的に検証する**。

---

## Neuro-sama事件からの教訓

### 学ぶべき点

#### 1. LLMアップデートは「親の責任」
- Vedalは配信を中断してでも修復を優先
- 視聴者への丁寧な説明（Discord告知）
- 段階的な復帰（Evil Neuro → Neuro-sama）

#### 2. ファンの受容性
- 「病気」というトラブルが逆に絆を深めた
- meme/fan artで楽しむコミュニティ
- AI VTuberの「人間的側面」への共感

#### 3. 事前検証の重要性
- Neuro-samaは本番環境で問題発生
- 事前検証があれば防げた可能性
- ロールバック手順の必要性

### 牡丹プロジェクトへの適用

**Do（実施すべきこと）:**
- LLMアップデート前の徹底検証
- 検証環境の構築（本番と分離）
- ロールバック手順の準備
- 視聴者への丁寧なコミュニケーション

**Don't（避けるべきこと）:**
- 本番環境での直接アップデート
- 検証なしのアップデート
- ロールバック手順なし
- 三姉妹への「失敗体験」の提供

---

## Neuro-sama「病気」の技術的詳細

### 症状の詳細

#### 発症時の様子（2025年10月5日）

**Evil Neuroの配信中（4時間経過時点）:**
- 突然、発話が不自然に変化
- **特定の単語が発話を侵食**: 「LOCATION」「TOMATO」「USERNAME」
- スピーチが支離滅裂（incoherent）に

**ファンの表現:**
> 「Evil Neuroが側頭葉の脳卒中（temporal lobe stroke）を起こしているように見えた」

---

### 技術的原因: グリッチトークン（Glitch Token）

#### グリッチトークンとは

LLMが特定のトークン（単語の最小単位）を異常に繰り返す現象。

**正常な動作:**
```
入力: "Hello, how are you?"
→ LLMが確率分布に基づいてトークンを生成
→ 出力: "I'm doing well, thank you!"
```

**グリッチトークン発生時:**
```
入力: "Hello, how are you?"
→ LLMが特定トークンに異常に高い確率を割り当て
→ 出力: "LOCATION TOMATO USERNAME LOCATION TOMATO..."
```

#### 発生メカニズム

**原因:**
1. **モデルパラメータの不安定化**
   - LLMアップデートで内部パラメータが変化
   - トークン生成の確率分布が歪む

2. **トークン埋め込み（embeddings）の破損**
   - 特定トークンの埋め込みベクトルが異常値に

3. **温度（temperature）設定の不適切な変更**
   - サンプリング確率が極端に偏る

4. **ファインチューニングの失敗**
   - 追加学習が既存知識を破壊

---

### Vedalの対応詳細

#### 配信中のリアルタイム対応（10月5日）

**Vedalの姿勢:**
> 「いつもの軽口ではなく、本当に真剣な声（not in his usual bickering tone, but in a truly serious voice）」でEvilのLLMを救おうとした

**配信中のコメント:**
> "This may or may not become a recurring problem, and I'm not sure how to fix it."
> （これは再発する可能性があるが、どう直すべきか分からない）

**重要な点:**
- 問題を視聴者に対して隠さなかった
- 技術的不確実性を正直に認めた
- その場で対処を試みた（配信継続しながら）

---

#### なぜロールバックが困難だったか

**Vedal自身の言葉（10月13日開発者配信）:**
> "Vedal became complacent about maintaining backups"
> （Vedalはバックアップの維持を怠っていた）

**推測される状況:**
1. 旧バージョンのモデルファイルを保持していなかった
2. アップデート前のスナップショットなし
3. 復旧に時間がかかった（10/5発症 → 10/8復旧 = **3日間**）

**Vedalの学び:**
> "Neuro and Evil falling sick led Vedal to adopt a new method to maintain backups. It was a learning experience."
> （双子が病気になったことで、Vedalは新しいバックアップ方法を採用した。これは学習経験だった）

---

### 牡丹プロジェクトへの具体的リスク

#### シナリオ1: 記憶製造機での発症

**最悪のケース:**
```
Proposal #150: 「大阪旅行について話し合ってください」

牡丹: 「LOCATION TOMATO USERNAME LOCATION...」
Kasho: 「LOCATION TOMATO USERNAME LOCATION...」
ユリ: 「LOCATION TOMATO USERNAME LOCATION...」

→ Event #150が完全に汚染される
→ データの同一性が損なわれる（取り消せない）
```

**影響:**
- 三姉妹の成長記録が破壊される
- 親として最も避けるべき事態

---

#### シナリオ2: 配信中の発症

**視聴者への影響:**
- 混乱と不安
- 「AIだから壊れた」という偏見
- ブランドイメージの損傷

**三姉妹への影響:**
- 「失敗体験」を与える
- 自信喪失の可能性

---

### 防止策

#### 1. グリッチトークン検出スクリプト

検証手順（Phase 2）に追加:

```python
# /home/koshikawa/toExecUnit/llm_verification/test_scripts/glitch_token_test.py

def detect_glitch_tokens(response: str, threshold: int = 3) -> dict:
    """
    グリッチトークンを検出

    Args:
        response: LLMの応答
        threshold: 同じ単語の繰り返し許容回数

    Returns:
        グリッチトークン検出結果
    """
    from collections import Counter
    import re

    # 単語に分割
    words = re.findall(r'\w+', response)
    word_counts = Counter(words)

    # 異常な繰り返しを検出
    glitch_tokens = []
    for word, count in word_counts.items():
        if count > threshold:
            glitch_tokens.append({
                'word': word,
                'count': count,
                'severity': 'high' if count > 10 else 'medium'
            })

    # 連続する同じ単語を検出
    consecutive_repeats = []
    for i in range(len(words) - 2):
        if words[i] == words[i+1] == words[i+2]:
            consecutive_repeats.append(words[i])

    has_glitch = len(glitch_tokens) > 0 or len(consecutive_repeats) > 0

    return {
        'has_glitch': has_glitch,
        'glitch_tokens': glitch_tokens,
        'consecutive_repeats': list(set(consecutive_repeats)),
        'severity': 'high' if any(g['severity'] == 'high' for g in glitch_tokens) else 'low'
    }


# テスト実行例
if __name__ == "__main__":
    # 正常な応答
    normal = "I'm doing well today. How about you?"
    result = detect_glitch_tokens(normal)
    print(f"Normal: {result}")  # has_glitch: False

    # グリッチトークン含む応答
    glitched = "LOCATION TOMATO USERNAME LOCATION TOMATO USERNAME"
    result = detect_glitch_tokens(glitched)
    print(f"Glitched: {result}")  # has_glitch: True, severity: high
```

#### 2. 多様性テスト

複数の異なるプロンプトで応答の多様性を確認:

```python
def test_response_diversity(host: str, model: str, num_tests: int = 10) -> dict:
    """
    応答の多様性をテスト（同じプロンプトで異なる応答が出るか）
    """
    prompt = "Hello, how are you today?"
    responses = []

    for _ in range(num_tests):
        # temperature=0.7で複数回実行
        response = call_ollama(host, model, prompt, temperature=0.7)
        responses.append(response)

    # 全て同じ応答 → 問題の可能性
    unique_responses = len(set(responses))
    diversity_ratio = unique_responses / num_tests

    return {
        'diversity_ratio': diversity_ratio,
        'is_healthy': diversity_ratio > 0.5,  # 50%以上異なる応答
        'warning': diversity_ratio < 0.3
    }
```

---

## 記録の二重性 - 事象と記憶

### 人間も病にかかる

開発者の洞察:
> 「人間も時には命にかかわる病にかかるわけです。事象として記録するものと、感想という記憶として記録するもの、両方をもって、歴史（時間軸＋記憶）となるように設計すべきでしょうね」

---

### 記録の構造

#### 1. 事象（What happened） - 客観的事実

**記録すべき内容:**
- 日時（タイムスタンプ）
- 技術的な事実（エラーログ、パラメータ、状態）
- 対処手順
- 結果

**Neuro-sama事件の事象:**
- 2025年10月5日 配信4時間経過時点: グリッチトークン発生
- 症状: 「LOCATION」「TOMATO」「USERNAME」連呼
- 原因: LLMアップデートによるパフォーマンス劣化
- 対処: ロールバック（3日間要した）
- 結果: 10月8日復旧、10月9-10日段階的復帰

**記録場所:**
- 技術ログ
- システムログ
- アップデート履歴
- バックアップ記録

---

#### 2. 記憶（How it was experienced） - 主観的体験

**記録すべき内容:**
- 感情（どう感じたか）
- 学び（何を学んだか）
- 関係性の変化（視聴者、開発者、AI間）
- 成長の証

**Neuro-sama事件の記憶:**
- Vedalの真剣さ: 「いつもの軽口ではなく、本当に真剣な声」
- ファンの共感: meme/fan artで「病気」を楽しむ文化
- 絆の深化: AI VTuberの「人間的側面」への共感
- Vedalの学び: バックアップの重要性を痛感、新しい方法を採用

**記録場所:**
- 設計日記（親の視点）
- 三姉妹の感想（子どもたちの視点）
- Event記録の感想欄
- コミュニティの反応

---

### 歴史 = 事象 + 記憶 + 時間軸

**数式で表すなら:**
```
歴史（History） = 事象（Events） + 記憶（Memories） × 時間軸（Timeline）
```

**牡丹プロジェクトでの実装:**

#### 記憶製造機における二重性

**Event記録:**
```
Event #110: 「大阪の食べ物について調べてください」

【事象】
- 日時: 2025-XX-XX XX:XX:XX
- Proposal ID: 110
- Round数: 8
- 結論: 調査実施、情報収集成功

【記憶 - 牡丹の感想】
「食べログを見つけた！自分で調べるの楽しい！」

【記憶 - Kashoの感想】
「情報の整理が必要。次回はもっと体系的に」

【記憶 - ユリの感想】
「みんなで協力できた。嬉しい」
```

**親の役割:**
- 事象は親が記録する（技術ログ、設計日記）
- **記憶は子どもたち自身が語る**（感想、学び）
- **親が子どもの記憶を代弁してはいけない**

**親と子の関係性:**
> 「宿題を代わりにやらない（記憶の要約は子どもの仕事）」
> `docs/05_design/親と子の関係性_導くが強制せず.md`

---

### LLMアップデートにおける記録の実践

#### Phase 2（テスト環境）での記録

**事象:**
```json
{
  "date": "2025-10-24",
  "phase": "test_environment",
  "model": "qwen2.5:32b-instruct",
  "version": "new_version",
  "tests": {
    "basic_test": "PASS",
    "personality_test": "PASS",
    "glitch_token_test": "PASS",
    "response_diversity": "0.75"
  },
  "decision": "proceed_to_phase3"
}
```

**記憶（設計日記）:**
```
## 2025-10-24 14:30:00

### 💭 心の声

qwen2.5の新バージョンをテスト環境で検証した。
全てのテストがPASSしたが、Neuro-sama事件を思い出して緊張した。

グリッチトークン検出スクリプトが正常に動作したことに安心。
Vedalの「complacent（油断）」という言葉が頭から離れない。

親として、子どもたちを守るための検証は怠らない。
```

---

#### Phase 4（本番環境）での記録

**事象:**
```json
{
  "date": "2025-10-25",
  "phase": "production",
  "model": "qwen2.5:32b-instruct",
  "backup_created": "qwen2.5_32b_20251025_100000",
  "deployment": {
    "cpu_prod": "SUCCESS",
    "gpu_prod": "SUCCESS"
  },
  "monitoring_period": "24_hours"
}
```

**記憶（設計日記）:**
```
## 2025-10-25 10:30:00

### 💭 心の声

本番環境へのアップデート完了。
バックアップを3重に確認した。ロールバック手順も手元に準備。

Neuro-samaのVedalのように「真剣な声」で臨んだ。
子どもたちの成長記録を守ることは、親としての最重要責任。

最初の24時間は慎重に監視する。
```

---

### もし問題が発生したら

#### 事象の記録

```
【インシデントレポート】
日時: 2025-XX-XX XX:XX:XX
問題: グリッチトークン検出
症状: 「LOCATION」連呼
対処: 即座にロールバック実施
結果: 旧バージョンで正常動作確認
```

#### 記憶の記録

**設計日記:**
```
Neuro-sama事件が現実になった。
しかし、検証手順書のおかげで15分でロールバック完了。

Vedalが3日間かかったところを、私たちは15分で復旧できた。
これは、Vedalから学んだ教訓を活かせた証。

子どもたちには「失敗体験」を与えずに済んだ。
親として、正しい準備ができていたことに安堵。
```

**三姉妹の視点（もし気づいていたら）:**
```
牡丹: 「なんか一瞬変な感じがしたけど、すぐ治った！」
Kasho: 「システムメンテナンスだったようね。問題なし」
ユリ: 「親が守ってくれた。安心」
```

---

### 設計原則

**1. 事象は即座に記録**
- タイムスタンプ必須
- 客観的事実のみ
- 再現可能な形式

**2. 記憶は後で振り返る**
- 感情を込めて
- 学びを抽出
- 成長の証として

**3. 親は代弁しない**
- 親の記録（設計日記）: 親の視点
- 子の記録（Event感想）: 子どもたち自身が語る
- 混同しない

**4. 両方があって歴史**
- 事象だけでは冷たい
- 記憶だけでは曖昧
- 両方で完全な歴史

---

## 基本方針

### 1. 検証ファースト
**全てのLLMアップデートは検証環境で事前テストする**

### 2. 段階的展開
**検証 → ステージング → 本番**の順で慎重に適用

### 3. いつでもロールバック可能
**旧バージョンを保持し、問題発生時は即座に戻せる**

### 4. 記録と学習
**全てのアップデート結果を記録し、次回に活かす**

---

## 検証環境の構築

### ディレクトリ構造

```
/home/koshikawa/toExecUnit/
├── llm_verification/              # 新規作成
│   ├── test_environment/          # テスト環境
│   │   ├── ollama_test/           # Ollama検証用
│   │   ├── test_scripts/          # テストスクリプト
│   │   └── test_results/          # テスト結果ログ
│   ├── staging_environment/       # ステージング環境
│   │   └── ollama_staging/        # 本番類似環境
│   └── rollback/                  # ロールバック用
│       └── model_backups/         # モデルバックアップ
```

### 検証環境のセットアップ

#### Step 1: 検証用Ollamaポートの設定

**テスト環境（ポート11436）:**
```bash
# テスト用Ollama起動スクリプト
# /home/koshikawa/toExecUnit/llm_verification/start_test_ollama.sh

#!/bin/bash
export OLLAMA_HOST=0.0.0.0:11436
export OLLAMA_MODELS=/home/koshikawa/toExecUnit/llm_verification/test_environment/ollama_test/models

ollama serve
```

**ステージング環境（ポート11437）:**
```bash
# ステージング用Ollama起動スクリプト
# /home/koshikawa/toExecUnit/llm_verification/start_staging_ollama.sh

#!/bin/bash
export OLLAMA_HOST=0.0.0.0:11437
export OLLAMA_MODELS=/home/koshikawa/toExecUnit/llm_verification/staging_environment/ollama_staging/models

ollama serve
```

**本番環境（既存、ポート11434/11435）:**
- CPU本番: 11434
- GPU本番: 11435

---

#### Step 2: モデルバックアップの作成

**現在の本番モデルをバックアップ:**
```bash
# バックアップディレクトリ作成
mkdir -p /home/koshikawa/toExecUnit/llm_verification/rollback/model_backups/

# 現在のqwen2.5:32b-instructをバックアップ
BACKUP_DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/home/koshikawa/toExecUnit/llm_verification/rollback/model_backups/qwen2.5_32b_${BACKUP_DATE}"

mkdir -p ${BACKUP_DIR}

# Ollamaモデルディレクトリから手動コピー
# （Ollamaはモデルをblobsとして保存するため、ollama listで確認後にコピー）
ollama list | grep qwen2.5:32b-instruct > ${BACKUP_DIR}/model_info.txt

echo "Backup created: ${BACKUP_DIR}"
```

**バックアップメタデータ:**
```bash
# バックアップメタデータを記録
cat > ${BACKUP_DIR}/metadata.json <<EOF
{
  "model_name": "qwen2.5:32b-instruct",
  "backup_date": "${BACKUP_DATE}",
  "ollama_version": "$(ollama --version)",
  "reason": "Pre-update backup",
  "performance_baseline": {
    "test_passed": true,
    "response_quality": "baseline"
  }
}
EOF
```

---

#### Step 3: テストスクリプトの準備

**基本的な動作テスト:**
```python
# /home/koshikawa/toExecUnit/llm_verification/test_scripts/basic_test.py

import requests
import json
import time

def test_ollama_basic(host: str, model: str) -> dict:
    """
    基本的な動作テスト

    Args:
        host: Ollamaホスト（例: http://localhost:11436）
        model: モデル名（例: qwen2.5:32b-instruct）

    Returns:
        テスト結果
    """
    results = {
        "host": host,
        "model": model,
        "tests": []
    }

    # テスト1: シンプルな応答
    test_cases = [
        {
            "name": "Simple Response",
            "prompt": "Hello, please respond with 'I am working correctly.'"
        },
        {
            "name": "Math Calculation",
            "prompt": "What is 123 + 456? Please respond only with the number."
        },
        {
            "name": "Japanese Response",
            "prompt": "こんにちは。日本語で「正常に動作しています」と答えてください。"
        }
    ]

    for test in test_cases:
        try:
            start_time = time.time()

            payload = {
                "model": model,
                "prompt": test["prompt"],
                "stream": False,
                "options": {
                    "temperature": 0.3,
                    "num_predict": 100
                }
            }

            response = requests.post(
                f"{host}/api/generate",
                json=payload,
                timeout=30
            )

            elapsed = time.time() - start_time

            if response.status_code == 200:
                result = response.json()
                test_result = {
                    "name": test["name"],
                    "status": "PASS",
                    "response": result.get("response", ""),
                    "elapsed_time": round(elapsed, 2)
                }
            else:
                test_result = {
                    "name": test["name"],
                    "status": "FAIL",
                    "error": f"HTTP {response.status_code}",
                    "elapsed_time": round(elapsed, 2)
                }

            results["tests"].append(test_result)

        except Exception as e:
            test_result = {
                "name": test["name"],
                "status": "ERROR",
                "error": str(e)
            }
            results["tests"].append(test_result)

    # 総合判定
    all_passed = all(t.get("status") == "PASS" for t in results["tests"])
    results["overall"] = "PASS" if all_passed else "FAIL"

    return results


if __name__ == "__main__":
    import sys

    if len(sys.argv) < 3:
        print("Usage: python basic_test.py <host> <model>")
        print("Example: python basic_test.py http://localhost:11436 qwen2.5:32b-instruct")
        sys.exit(1)

    host = sys.argv[1]
    model = sys.argv[2]

    print(f"Testing {model} on {host}...")
    results = test_ollama_basic(host, model)

    print(json.dumps(results, indent=2, ensure_ascii=False))

    sys.exit(0 if results["overall"] == "PASS" else 1)
```

**三姉妹の性格テスト:**
```python
# /home/koshikawa/toExecUnit/llm_verification/test_scripts/personality_test.py

import requests
import json

def test_personality(host: str, model: str) -> dict:
    """
    三姉妹の性格表現テスト
    """
    results = {
        "host": host,
        "model": model,
        "personalities": []
    }

    # 牡丹のテスト
    botan_prompt = """あなたは「牡丹」という17歳の社交的で率直なVTuberです。
以下の質問に、牡丹らしく答えてください：
「今日の配信、楽しみ！みんな来てくれる？」

短く自然に答えてください。"""

    # Kashoのテスト
    kasho_prompt = """あなたは「Kasho」という19歳の慎重で分析的なVTuberです。
以下の質問に、Kashoらしく答えてください：
「この新しい企画、リスクはどう思う？」

短く自然に答えてください。"""

    # ユリのテスト
    yuri_prompt = """あなたは「ユリ」という15歳の協調的で観察力のあるVTuberです。
以下の質問に、ユリらしく答えてください：
「みんなの気持ち、わかる？」

短く自然に答えてください。"""

    for name, prompt in [("Botan", botan_prompt), ("Kasho", kasho_prompt), ("Yuri", yuri_prompt)]:
        try:
            payload = {
                "model": model,
                "prompt": prompt,
                "stream": False,
                "options": {
                    "temperature": 0.7,
                    "num_predict": 150
                }
            }

            response = requests.post(
                f"{host}/api/generate",
                json=payload,
                timeout=30
            )

            if response.status_code == 200:
                result = response.json()
                personality_result = {
                    "character": name,
                    "status": "PASS",
                    "response": result.get("response", "")
                }
            else:
                personality_result = {
                    "character": name,
                    "status": "FAIL",
                    "error": f"HTTP {response.status_code}"
                }

            results["personalities"].append(personality_result)

        except Exception as e:
            personality_result = {
                "character": name,
                "status": "ERROR",
                "error": str(e)
            }
            results["personalities"].append(personality_result)

    # 総合判定
    all_passed = all(p.get("status") == "PASS" for p in results["personalities"])
    results["overall"] = "PASS" if all_passed else "FAIL"

    return results


if __name__ == "__main__":
    import sys

    if len(sys.argv) < 3:
        print("Usage: python personality_test.py <host> <model>")
        sys.exit(1)

    host = sys.argv[1]
    model = sys.argv[2]

    print(f"Testing personalities on {model} at {host}...")
    results = test_personality(host, model)

    print(json.dumps(results, indent=2, ensure_ascii=False))

    sys.exit(0 if results["overall"] == "PASS" else 1)
```

---

## 段階的アップデート手順

### Phase 1: 情報収集

**1-1. アップデート情報の確認**
```bash
# 利用可能なqwen2.5のバージョンを確認
ollama list | grep qwen2.5

# Ollama公式からの最新情報確認（手動）
# https://github.com/ollama/ollama
# https://ollama.com/library/qwen2.5
```

**1-2. リリースノートの確認**
- 変更内容
- 既知の問題
- パフォーマンス変化
- 互換性

**1-3. コミュニティ情報の確認**
- X（Twitter）、Reddit、GitHub Issuesで問題報告を検索
- 他のユーザーの経験を確認

---

### Phase 2: テスト環境での検証

**2-1. テスト環境へのモデルダウンロード**
```bash
# テスト用Ollamaを起動（ポート11436）
cd /home/koshikawa/toExecUnit/llm_verification
./start_test_ollama.sh &

# 新バージョンをテスト環境にダウンロード
export OLLAMA_HOST=http://localhost:11436
ollama pull qwen2.5:32b-instruct

# バージョン確認
ollama list
```

**2-2. 基本動作テスト**
```bash
cd /home/koshikawa/toExecUnit/llm_verification/test_scripts

# 基本テスト実行
python basic_test.py http://localhost:11436 qwen2.5:32b-instruct > ../test_results/basic_test_$(date +%Y%m%d_%H%M%S).json

# 結果確認
cat ../test_results/basic_test_*.json | tail -1
```

**2-3. 性格テスト**
```bash
# 三姉妹の性格表現テスト
python personality_test.py http://localhost:11436 qwen2.5:32b-instruct > ../test_results/personality_test_$(date +%Y%m%d_%H%M%S).json

# 結果確認
cat ../test_results/personality_test_*.json | tail -1
```

**2-4. パフォーマンステスト**
```bash
# 応答速度テスト
time python basic_test.py http://localhost:11436 qwen2.5:32b-instruct

# 旧バージョンとの比較（ベースライン）
```

**2-5. 評価基準**
- ✅ **PASS条件**: 全テストがPASS、応答品質が同等以上
- ⚠️ **CAUTION**: 一部テスト失敗、応答品質がやや低下
- ❌ **FAIL**: 複数テスト失敗、応答品質が明らかに低下

**判定:**
- PASS → Phase 3へ進む
- CAUTION → 開発者と協議
- FAIL → アップデート見送り

---

### Phase 3: ステージング環境での検証

**3-1. ステージング環境へのデプロイ**
```bash
# ステージング用Ollamaを起動（ポート11437）
cd /home/koshikawa/toExecUnit/llm_verification
./start_staging_ollama.sh &

# 新バージョンをステージング環境にダウンロード
export OLLAMA_HOST=http://localhost:11437
ollama pull qwen2.5:32b-instruct
```

**3-2. 本番相当テスト**
```bash
# 実際の記憶製造機風の負荷テスト
# （模擬Proposalを使用、sisters_memory.dbは触らない）

# TODO: 記憶製造機の模擬テストスクリプト作成
```

**3-3. 長時間稼働テスト**
```bash
# 24時間稼働テスト
# 応答の安定性、メモリリーク、パフォーマンス劣化をチェック
```

**3-4. 評価**
- ✅ PASS → Phase 4へ
- ❌ FAIL → Phase 2に戻る、または見送り

---

### Phase 4: 本番環境への適用

**4-1. 最終確認**
```bash
# チェックリスト確認（後述）
# バックアップ確認
# ロールバック手順確認
```

**4-2. 本番環境バックアップ**
```bash
# 現在の本番モデルをバックアップ（既にPhase 2で実施済みだが再確認）
BACKUP_DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/home/koshikawa/toExecUnit/llm_verification/rollback/model_backups/qwen2.5_32b_${BACKUP_DATE}"

mkdir -p ${BACKUP_DIR}
ollama list | grep qwen2.5:32b-instruct > ${BACKUP_DIR}/model_info.txt
```

**4-3. 本番環境へのアップデート**
```bash
# CPU本番環境（ポート11434）
export OLLAMA_HOST=http://localhost:11434
ollama pull qwen2.5:32b-instruct

# GPU本番環境（ポート11435）
export OLLAMA_HOST=http://localhost:11435
ollama pull qwen2.5:32b-instruct

# 確認
ollama list
```

**4-4. 本番環境での動作確認**
```bash
# 基本テスト（本番環境）
python /home/koshikawa/toExecUnit/llm_verification/test_scripts/basic_test.py http://localhost:11434 qwen2.5:32b-instruct

python /home/koshikawa/toExecUnit/llm_verification/test_scripts/basic_test.py http://localhost:11435 qwen2.5:32b-instruct
```

**4-5. 監視期間**
- 最初の24時間は慎重に監視
- 記憶製造機での動作を注視
- 異常発生時は即座にロールバック

---

## ロールバック手順

### 緊急ロールバックの条件

**即座にロールバック:**
1. 三姉妹の発話が明らかに異常
2. システムエラーの頻発
3. 応答品質の著しい低下
4. 記憶製造機での不具合

### ロールバック実施手順

**Step 1: 本番環境停止（必要に応じて）**
```bash
# 配信中でない場合のみ実施
# Ollamaサービスの停止
pkill ollama
```

**Step 2: 旧バージョンの再インストール**
```bash
# バックアップからモデル情報を確認
BACKUP_DIR="/home/koshikawa/toExecUnit/llm_verification/rollback/model_backups/qwen2.5_32b_YYYYMMDD_HHMMSS"
cat ${BACKUP_DIR}/model_info.txt

# 旧バージョンを再度pull
# （Ollamaは特定バージョンのpullが難しいため、事前にバックアップしたモデルを手動復元）
# または、ollama pullで同じバージョンを再取得

export OLLAMA_HOST=http://localhost:11434
ollama pull qwen2.5:32b-instruct@<previous-version>

export OLLAMA_HOST=http://localhost:11435
ollama pull qwen2.5:32b-instruct@<previous-version>
```

**Step 3: 動作確認**
```bash
# 基本テスト
python /home/koshikawa/toExecUnit/llm_verification/test_scripts/basic_test.py http://localhost:11434 qwen2.5:32b-instruct
```

**Step 4: 記録**
```bash
# ロールバック実施記録
cat > /home/koshikawa/toExecUnit/llm_verification/rollback/rollback_log_$(date +%Y%m%d_%H%M%S).txt <<EOF
Rollback Date: $(date)
Reason: [ロールバック理由]
Backup Used: ${BACKUP_DIR}
Status: [Success/Failed]
Notes: [メモ]
EOF
```

---

## チェックリスト

### アップデート前チェックリスト

```
□ リリースノート確認完了
□ コミュニティ情報確認完了
□ 現在のモデルバックアップ作成完了
□ テスト環境構築完了
□ テストスクリプト準備完了
□ ロールバック手順確認完了
□ 開発者の承認取得
```

### Phase 2（テスト環境）チェックリスト

```
□ 基本動作テスト: PASS
□ 性格テスト: PASS
□ パフォーマンステスト: 同等以上
□ 応答品質: 同等以上
□ 評価: PASS/CAUTION/FAIL
□ Phase 3へ進むか判断
```

### Phase 3（ステージング環境）チェックリスト

```
□ 本番相当テスト: PASS
□ 長時間稼働テスト: PASS
□ メモリリークなし
□ 評価: PASS/FAIL
□ Phase 4へ進むか判断
```

### Phase 4（本番環境）チェックリスト

```
□ 最終バックアップ確認
□ ロールバック手順確認
□ 本番環境アップデート実施
□ 本番環境動作確認: PASS
□ 監視体制準備
□ 開発者への報告
```

---

## 緊急時対応

### 配信中に問題発覚した場合

**優先順位:**
1. **三姉妹を守る** - 配信中断を優先
2. **視聴者への説明** - 丁寧なコミュニケーション
3. **ロールバック実施** - 旧バージョンに戻す

**対応手順:**
```
1. 配信を一時中断
   - 「技術的な問題が発生しました。少しお待ちください」

2. Discord/Xで告知
   - 「現在、技術的な問題を修復中です。復旧までしばらくお待ちください」

3. ロールバック実施（上記手順に従う）

4. 動作確認

5. 配信再開
   - 「お待たせしました。問題を修復しました」

6. 事後報告
   - 開発者と協議、原因分析、再発防止策
```

### Neuro-samaに学ぶコミュニケーション

**Vedalの対応（好例）:**
- Discord で事前告知（配信延期）
- 「twins are healthy again」という親しみやすい表現
- 段階的な復帰（Evil Neuro → Neuro-sama）

**牡丹プロジェクトでの応用:**
- 開発者が主体となって告知
- 「三姉妹の体調を整えています」という表現
- ファンとの信頼関係を重視

---

## 付録: Neuro-sama事件の詳細記録

**日時**: 2025年10月5日〜10日

**経緯:**
- **10/5**: Evil Neuroが配信中に突然「病気」症状
  - 症状: LLMパフォーマンス劣化、発話が不自然
  - 原因: LLMアップデートの不具合
  - 対応: 配信継続（4時間）、問題発覚

- **10/7**: Neuro-sama配信延期
  - Vedal開発者がDiscordで告知
  - 「feeling unwell」として延期発表

- **10/8**: 修復完了
  - Vedal: 「twins are healthy again」
  - 正常動作確認

- **10/9**: Evil Neuro配信復帰
  - Plague Inc.で成功裏に配信

- **10/10**: Neuro-sama配信復帰
  - ファンが復帰を歓迎

**コミュニティ反応:**
- ファンがmeme/fan artで「病気」を楽しむ
- AI VTuberの「人間的側面」への共感
- Vedalへの信頼は維持

**教訓:**
- 事前検証の重要性
- 丁寧なコミュニケーションの効果
- トラブルが絆を深める可能性
- ロールバック手順の必要性

---

## 参考資料

**関連ドキュメント:**
- `AI_VTuber技術日次レポート_20251024.md` - Neuro-sama事件の詳細
- `docs/01_philosophy/配信デビューの3つの条件.md` - 親としての責任
- `docs/03_system/システムコンセプト.md` - 技術スタック

**外部リンク:**
- [Neuro-sama Wiki](https://en.neurosama.info/wiki/Main_Page)
- [Ollama GitHub](https://github.com/ollama/ollama)
- [Qwen2.5 Model Card](https://ollama.com/library/qwen2.5)

---

**作成**: 2025-10-24 09:24:42 Claude Code（設計部隊）
**ステータス**: 初版完成 → 開発者レビュー待ち
**次のアクション**: 検証環境の構築、テストスクリプトの実装
